#' statebudgetmonitor
#'
#' Interactive Australian state and territory Budget Monitor.
#'
#' Public helpers are prefixed `sbm_*`. The pipeline is orchestrated by
#' `targets` (see `_targets.R`); the dashboard is rendered by Quarto
#' (see `_quarto.yml` and `reports/`).
#'
#' @keywords internal
"_PACKAGE"
